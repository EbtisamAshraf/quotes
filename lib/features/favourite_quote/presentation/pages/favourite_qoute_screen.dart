import 'package:flutter/material.dart';

class FavouriteQouteScreen extends StatelessWidget {
  const FavouriteQouteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body:   Center(child: Text('Favourite Qoute'),) 
  
    );
  }
}
